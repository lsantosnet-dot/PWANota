# PromptNota - PWA Prompt Manager

App PWA para gerenciar prompts e enviar ao Gemini via Web Share API.

## Rodar localmente

```
npx -y http-server . -p 5173 --cors -c-1
```

Ou dê duplo-clique em `start.bat`.
